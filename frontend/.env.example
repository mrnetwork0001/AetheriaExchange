# AI Co-Pilot intent engine (server-side only — never exposed to the browser).
# Without a key the API falls back to a deterministic offline parser.
ANTHROPIC_API_KEY=

# OKX DEX Aggregator API credentials (https://web3.okx.com/build/dev-portal).
# Required for live 1-click swap routing; without them DEX routing is disabled.
OKX_API_KEY=
OKX_API_SECRET=
OKX_API_PASSPHRASE=
OKX_PROJECT_ID=
